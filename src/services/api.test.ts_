import { ActionsObservable } from "redux-observable";

describe("api test", () => {
  it("should return the venue photos", () => {
    const action$ = ActionsObservable.of({
      type: "VENUE_PHOTOS",
      queryId: "1"
    });
    venuePhotoEpic(action$);
  });
});
